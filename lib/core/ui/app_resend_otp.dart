import 'package:flutter/cupertino.dart';

class AppResendOtp extends StatelessWidget {
  const AppResendOtp({super.key});

  @override
  Widget build(BuildContext context) {
    return Row(children: []);
  }
}
